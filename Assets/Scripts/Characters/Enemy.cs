using UnityEngine;

public class Enemy : Character
{
    public EnemyData sourceData; //Links back to the ScriptableObject

    //TODO: (Core Combat Logic phase): add "public Intent CurrentIntent;" once the
    //Intent struct (type, value, target, icon) is defined. L
}
